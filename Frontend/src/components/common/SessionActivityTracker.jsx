import { useEffect } from 'react';
import { useAuth } from '../../hooks/useAuth.jsx';

// Session Activity Tracker - DISABLED
// This component has been disabled as session expiration is turned off
const SessionActivityTracker = () => {
  // Activity tracking is disabled
  return null;
};

export default SessionActivityTracker;